/**
 * User role enumeration
 */
export enum Role {
  ADMIN = 'admin',
  USER = 'user'
}

/**
 * User status enumeration
 */
export enum Status {
  ACTIVE = 'active',
  BAN = 'ban',
  DELETED = 'deleted'
}